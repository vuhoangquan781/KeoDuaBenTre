@model IEnumerable<WEBKEODUA.Models.SANPHAM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/admin/Views/Shared/_LayoutPage1.cshtml";
}

<h2>Quản Lý Danh Sách Sản Phẩm</h2>

<p>
    @Html.ActionLink("Thêm mới sản phẩm", "Create")
</p>
<table class="table">
    <tr>
        <th>
            
            <label>Tên Sản Phẩm</label>
        </th>
        <th>
            <label>Trọng Lượng</label>
        </th>
        <th>
            <label>Quy Cách</label>
        </th>
        <th>
            <label>Khuyến Mãi</label>
        </th>
        <th>
            <label>Giá Bán</label>
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.CTSANPHAM.ANH)
        </th>*@
        <th>
            <label>Tên Loại Sản Phẩm</label>
        </th>
    </tr>
    
@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TRONGLUONG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QUYCACH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KHUYENMAI)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GIABAN)
        </td>
        
        @*<td>
            <img src="~/Image/@item.CTSANPHAM.ANH" style="width:370px;height:265px" alt="Alternate Text" />
            
            @*<img class="card-header" src="@Url.Content("~/Image/"+ item.CTSANPHAM.ANH)" style="width:370px;height:265px"  alt="Image" />
            
        </td>*@
        <td>
            @Html.DisplayFor(modelItem => item.LOAISP.TENLOAISP)
        </td>
        <td>
            @Html.ActionLink("Sửa Thông Tin", "Edit", new { id = item.MASP }) |
            @Html.ActionLink("Chi tiết", "Details", new { id = item.MASP }) |
            @Html.ActionLink("Xóa", "Delete", new { id = item.MASP })
        </td>
    </tr>
}
</table>

@*<table class="table">
    <thead>
        <tr>
           
            <th scope="col">ảnh</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                
                <th>
                    <img src="~/Image/@item.CTSANPHAM.ANH" alt="Alternate Text" />
                </th>
            </tr>
          
        }
        
    </tbody>
</table>*@